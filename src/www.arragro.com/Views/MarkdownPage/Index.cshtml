@model arragro.com.PageTypes.MarkdownPage

<div class="row">
    <div class="col-md-12">
        <h3>@Model.Title</h3>

        <p>@Html.Raw(@CommonMark.CommonMarkConverter.Convert(Html.Encode(Model.Markdown).Replace("&#xA;", "\n")))</p>
    </div>
</div>